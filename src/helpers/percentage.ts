export function getPercentageFromWidth (percentage : number) {
    return percentage / 100 * window.innerWidth     
}

export function getPercentageFromHeight (percentage : number) {
    return percentage / 100 * window.innerHeight
}